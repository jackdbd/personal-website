<div class="cluster">
    <ul class="p-2 cluster-children-wrapper">
        {% for entry in metadata.socials %}
        <li class="">
            <a href="{{ entry.url | url }}" rel="noopener noreferrer" target="_blank">
                {{ entry.name }}
            </a>
        </li>
        {% endfor %}
    </ul>
</div>
